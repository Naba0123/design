<%= form_for @guidances_user, :html => { class: "form-horizontal" } do |f| %>
  <% if @guidances_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guidances_user.errors.count, "error") %> prohibited this guidances_user from being saved:</h2>

      <ul>
      <% @guidances_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :user_id, class: "col-sm-2 control-label"%>
    <div class="col-sm-10">
      <%= f.number_field :user_id, class: "form-control"%>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :guidance_id, class: "col-sm-2 control-label"%>
    <div class="col-sm-10">
      <%= f.number_field :guidance_id, class: "form-control"%>
    </div>
  </div>
  <div class="actions">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary"%>
    </div>
  </div>
<% end %>
