<%= form_for(@guidance) do |f| %>
  <% if @guidance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@guidance.errors.count, "error") %> prohibited this guidance from being saved:</h2>

      <ul>
      <% @guidance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :guidance_date %><br>
    <%= f.date_select :guidance_date %>
  </div>
  <div class="field">
    <%= f.label :guidance_place %><br>
    <%= f.text_field :guidance_place %>
  </div>
  <div class="form-group">
    <%= f.label :guidance_kind_id, t('activerecord.attributes.guidance.guidance_kind'), class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
    <%= f.collection_select :guidance_kind_id, GuidanceKind.all, :id, :name, { prompt: "Please choose... " }, { class: "form-control" } %>
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
